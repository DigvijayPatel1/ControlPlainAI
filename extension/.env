# Backend API base URL the extension's background service worker calls.
# Must match backend.env's port / frontend.env's VITE_API_BASE backend target.
VITE_API_BASE=http://127.0.0.1:8000
 
# Dashboard URL the extension opens/connects to. Must match frontend.env's dev server.
VITE_DASHBOARD_URL=http://localhost:5173
 